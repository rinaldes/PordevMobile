import React from 'react';
import { Layout, Text } from '@ui-kitten/components';

const ArticleDetail = () => (
    <Layout style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <Text category='h1'>ArticleDetail</Text>
    </Layout>
);

export default ArticleDetail;